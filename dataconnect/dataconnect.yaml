specVersion: "v1"
serviceId: "personal-site-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "personal-site-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
